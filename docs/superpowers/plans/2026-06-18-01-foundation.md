# 보통의 정원 웹앱 — 기반(Foundation) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development (recommended) or superpowers:executing-plans 로 태스크별 구현. 스텝은 체크박스(`- [ ]`) 추적.

**Goal:** 빌드·실행·배포되는 빈 앱 셸 — 3탭 네비게이션, 디자인 토큰(라이트/다크), Dexie 로컬 DB(스키마 + areas CRUD), 설치 가능한 PWA, Vitest·빌드·GitHub Pages 파이프라인.

**Architecture:** 순수 정적 React SPA. 계층 분리(data / sync / domain / ui). 이 계획은 **ui 셸 + data 기반(Dexie 스키마·리포지토리 패턴)**만 세운다. 기능(날씨·정원·일지·케어·동기화)은 후속 계획 ②~⑥.

**Tech Stack:** React 18 · Vite · TypeScript · react-router-dom(HashRouter) · Dexie · vite-plugin-pwa · Vitest · GitHub Actions/Pages

**작업 경로:** 모든 파일은 `/Users/rakonteto/claude-workspace/ordinary-garden-web/` 기준.

---

## File Structure
- `package.json` — 의존성·스크립트
- `vite.config.ts` — Vite + React + PWA + Vitest + base 경로
- `tsconfig.json`, `tsconfig.node.json` — TS 설정
- `index.html` — 진입 HTML, PWA meta
- `.gitignore`
- `src/main.tsx` — React 진입(CSS·App 마운트)
- `src/App.tsx` — HashRouter + 라우트 + 레이아웃
- `src/vite-env.d.ts` — Vite 타입
- `src/ui/tokens.css` — 디자인 토큰(CSS 변수, 라이트/다크)
- `src/ui/global.css` — reset·글로벌·safe-area
- `src/ui/TabBar.tsx` / `TabBar.css` — 하단 탭바
- `src/routes/TodayRoute.tsx` / `GardenRoute.tsx` / `SettingsRoute.tsx` — 빈 화면
- `src/data/types.ts` — 레코드 타입
- `src/data/record.ts` — id·공통필드 헬퍼
- `src/data/db.ts` — Dexie 인스턴스·스키마
- `src/data/areas.ts` — areas 리포지토리(CRUD 패턴 확립)
- `src/test/setup.ts` — 테스트 셋업
- `.github/workflows/deploy.yml` — Pages 배포

---

## Task 1: 프로젝트 scaffold

**Files:** Create `package.json`, `tsconfig.json`, `tsconfig.node.json`, `vite.config.ts`, `index.html`, `.gitignore`, `src/main.tsx`, `src/App.tsx`, `src/vite-env.d.ts`

> 디렉토리에 `docs/`·`.git`이 이미 있어 `npm create vite`(대화형)를 피하고 파일을 직접 생성한 뒤 `npm install` 한다.

- [ ] **Step 1: package.json 작성**
```json
{
  "name": "ordinary-garden-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  }
}
```

- [ ] **Step 2: 의존성 설치** (버전은 npm이 최신으로 잠금)
```bash
cd /Users/rakonteto/claude-workspace/ordinary-garden-web
npm install react react-dom react-router-dom dexie
npm install -D vite @vitejs/plugin-react typescript @types/react @types/react-dom
```
Expected: `node_modules/`·`package-lock.json` 생성, 에러 없음.

- [ ] **Step 3: 설정·진입 파일 작성**

`tsconfig.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "useDefineForClassFields": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

`tsconfig.node.json`:
```json
{
  "compilerOptions": {
    "composite": true,
    "skipLibCheck": true,
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowSyntheticDefaultImports": true,
    "strict": true
  },
  "include": ["vite.config.ts"]
}
```

`vite.config.ts` (base·PWA·test는 후속 태스크에서 추가):
```ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
})
```

`index.html`:
```html
<!doctype html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
    <title>보통의 정원</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

`src/vite-env.d.ts`:
```ts
/// <reference types="vite/client" />
```

`src/App.tsx` (임시 — Task 4에서 교체):
```tsx
export default function App() {
  return <div>보통의 정원</div>
}
```

`src/main.tsx`:
```tsx
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import App from './App'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
```

`.gitignore`:
```
node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
coverage
```

- [ ] **Step 4: 빌드 확인**
Run: `npm run build`
Expected: `dist/` 생성, 타입 에러 없이 성공.

- [ ] **Step 5: 커밋**
```bash
git add -A
git commit -m "feat(web): Vite+React+TS 프로젝트 기반"
```

---

## Task 2: Vitest 셋업

**Files:** Modify `vite.config.ts` · Create `src/test/setup.ts`, `src/test/smoke.test.ts`

- [ ] **Step 1: 테스트 의존성 설치**
```bash
npm install -D vitest jsdom @testing-library/react @testing-library/dom @testing-library/jest-dom @testing-library/user-event
```

- [ ] **Step 2: vite.config.ts에 test 설정 추가**
```ts
/// <reference types="vitest/config" />
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./src/test/setup.ts'],
  },
})
```

- [ ] **Step 3: setup·smoke 테스트 작성**

`src/test/setup.ts`:
```ts
import '@testing-library/jest-dom/vitest'
```

`src/test/smoke.test.ts`:
```ts
import { describe, it, expect } from 'vitest'

describe('smoke', () => {
  it('테스트 러너가 동작한다', () => {
    expect(1 + 1).toBe(2)
  })
})
```

- [ ] **Step 4: 테스트 실행**
Run: `npm test`
Expected: 1 passed.

- [ ] **Step 5: 커밋**
```bash
git add -A
git commit -m "test(web): Vitest+Testing Library 셋업"
```

---

## Task 3: 디자인 토큰 + 글로벌 CSS

> 세이지 배경·포레스트그린·글래스, 라이트/다크. 아래 hex는 **시작값** — 구현·검증 단계에서 SwiftUI 원본(`ordinary-garden`)과 시각 대조해 미세조정한다.

**Files:** Create `src/ui/tokens.css`, `src/ui/global.css` · Modify `src/main.tsx`

- [ ] **Step 1: tokens.css 작성**
```css
:root {
  --color-bg: #eef1ea;
  --color-surface: rgba(255, 255, 255, 0.72);
  --color-surface-border: rgba(255, 255, 255, 0.5);
  --color-primary: #2f5d3a;
  --color-primary-weak: #6b8e6e;
  --color-text: #20271f;
  --color-text-weak: #5a6357;
  --color-accent: #c9d6bf;
  --radius-card: 18px;
  --radius-chip: 999px;
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 24px;
  --shadow-card: 0 4px 20px rgba(40, 60, 40, 0.08);
  --blur-glass: 14px;
  --tabbar-height: 64px;
}

@media (prefers-color-scheme: dark) {
  :root {
    --color-bg: #161a15;
    --color-surface: rgba(40, 48, 40, 0.6);
    --color-surface-border: rgba(255, 255, 255, 0.08);
    --color-primary: #8fc99a;
    --color-primary-weak: #6f9a78;
    --color-text: #e7ece4;
    --color-text-weak: #a7b0a2;
    --color-accent: #34412f;
    --shadow-card: 0 4px 20px rgba(0, 0, 0, 0.3);
  }
}

[data-theme='light'] { color-scheme: light; }
[data-theme='dark'] { color-scheme: dark; }
```
(수동 토글 `[data-theme]`는 설정 단계에서 활용. 기반은 시스템 연동만으로 동작.)

- [ ] **Step 2: global.css 작성**
```css
*, *::before, *::after { box-sizing: border-box; }
html, body, #root { height: 100%; margin: 0; }
body {
  background: var(--color-bg);
  color: var(--color-text);
  font-family: -apple-system, BlinkMacSystemFont, 'Pretendard', 'Apple SD Gothic Neo', system-ui, sans-serif;
  font-weight: 400;
  -webkit-font-smoothing: antialiased;
}
#root {
  min-height: 100dvh;
  display: flex;
  flex-direction: column;
}
h1, h2, h3 { font-weight: 600; margin: 0; }
button { font: inherit; color: inherit; }
```

- [ ] **Step 3: main.tsx에서 CSS import**
```tsx
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './ui/tokens.css'
import './ui/global.css'
import App from './App'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
```

- [ ] **Step 4: 시각 확인**
Run: `npm run dev` → 브라우저에서 세이지 배경 + OS 다크모드 전환 시 색 반전 확인.

- [ ] **Step 5: 커밋**
```bash
git add -A
git commit -m "feat(web): 디자인 토큰·글로벌 CSS(라이트/다크)"
```

---

## Task 4: 앱 셸 + 라우팅 + 탭바

**Files:** Create `src/routes/{TodayRoute,GardenRoute,SettingsRoute}.tsx`, `src/ui/TabBar.tsx`, `src/ui/TabBar.css`, `src/App.test.tsx` · Modify `src/App.tsx`

- [ ] **Step 1: 실패하는 테스트 작성**

`src/App.test.tsx`:
```tsx
import { render, screen } from '@testing-library/react'
import { describe, it, expect } from 'vitest'
import App from './App'

describe('App 셸', () => {
  it('세 탭(오늘·내 정원·설정)을 렌더한다', () => {
    render(<App />)
    expect(screen.getByRole('link', { name: '오늘' })).toBeInTheDocument()
    expect(screen.getByRole('link', { name: '내 정원' })).toBeInTheDocument()
    expect(screen.getByRole('link', { name: '설정' })).toBeInTheDocument()
  })

  it('기본 경로에서 오늘 화면을 보여준다', () => {
    render(<App />)
    expect(screen.getByRole('heading', { name: '오늘' })).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: 실패 확인**
Run: `npm test`
Expected: FAIL (App이 탭/라우트 미구현).

- [ ] **Step 3: 라우트·탭바·App 구현**

`src/routes/TodayRoute.tsx`:
```tsx
export default function TodayRoute() {
  return (
    <section style={{ padding: 'var(--space-4)' }}>
      <h1>오늘</h1>
    </section>
  )
}
```

`src/routes/GardenRoute.tsx` (동일 패턴, heading '내 정원'):
```tsx
export default function GardenRoute() {
  return (
    <section style={{ padding: 'var(--space-4)' }}>
      <h1>내 정원</h1>
    </section>
  )
}
```

`src/routes/SettingsRoute.tsx` (동일 패턴, heading '설정'):
```tsx
export default function SettingsRoute() {
  return (
    <section style={{ padding: 'var(--space-4)' }}>
      <h1>설정</h1>
    </section>
  )
}
```

`src/ui/TabBar.tsx`:
```tsx
import { NavLink } from 'react-router-dom'
import './TabBar.css'

const TABS = [
  { to: '/today', label: '오늘' },
  { to: '/garden', label: '내 정원' },
  { to: '/settings', label: '설정' },
]

export default function TabBar() {
  return (
    <nav className="tabbar">
      {TABS.map((t) => (
        <NavLink key={t.to} to={t.to} className="tabbar__item">
          {t.label}
        </NavLink>
      ))}
    </nav>
  )
}
```

`src/ui/TabBar.css`:
```css
.tabbar {
  position: sticky;
  bottom: 0;
  display: flex;
  height: calc(var(--tabbar-height) + env(safe-area-inset-bottom));
  padding-bottom: env(safe-area-inset-bottom);
  background: var(--color-surface);
  backdrop-filter: blur(var(--blur-glass));
  -webkit-backdrop-filter: blur(var(--blur-glass));
  border-top: 1px solid var(--color-surface-border);
}
.tabbar__item {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  text-decoration: none;
  color: var(--color-text-weak);
  font-size: 13px;
}
.tabbar__item.active {
  color: var(--color-primary);
  font-weight: 600;
}
```

`src/App.tsx`:
```tsx
import { HashRouter, Routes, Route, Navigate } from 'react-router-dom'
import TabBar from './ui/TabBar'
import TodayRoute from './routes/TodayRoute'
import GardenRoute from './routes/GardenRoute'
import SettingsRoute from './routes/SettingsRoute'

export default function App() {
  return (
    <HashRouter>
      <main style={{ flex: 1, overflowY: 'auto' }}>
        <Routes>
          <Route path="/today" element={<TodayRoute />} />
          <Route path="/garden" element={<GardenRoute />} />
          <Route path="/settings" element={<SettingsRoute />} />
          <Route path="*" element={<Navigate to="/today" replace />} />
        </Routes>
      </main>
      <TabBar />
    </HashRouter>
  )
}
```

- [ ] **Step 4: 통과 확인**
Run: `npm test`
Expected: PASS. (기본 경로 `*`→`/today` 리다이렉트로 heading '오늘' 노출. NavLink active 클래스는 react-router가 자동 부여.)

- [ ] **Step 5: 커밋**
```bash
git add -A
git commit -m "feat(web): 앱 셸·HashRouter·3탭 네비게이션"
```

---

## Task 5: Dexie 스키마 + 리포지토리

**Files:** Create `src/data/types.ts`, `src/data/record.ts`, `src/data/db.ts`, `src/data/areas.ts`, `src/data/areas.test.ts` · Modify `src/test/setup.ts`

- [ ] **Step 1: fake-indexeddb 설치 + setup 갱신**
```bash
npm install -D fake-indexeddb
```
`src/test/setup.ts`:
```ts
import '@testing-library/jest-dom/vitest'
import 'fake-indexeddb/auto'
```

- [ ] **Step 2: 타입·헬퍼·DB 정의**

`src/data/types.ts`:
```ts
export interface BaseRecord {
  id: string
  updatedAt: number  // epoch ms
  deleted: boolean   // tombstone
}

export type LightRequirement = 'high' | 'medium' | 'low'
export type CareType = 'water' | 'fertilize' | 'repot'

export interface Area extends BaseRecord {
  name: string
  sortOrder: number
}

export interface Plant extends BaseRecord {
  areaId: string
  name: string
  lightRequirement: LightRequirement
  photoId?: string
  datePlanted?: number
  note?: string
  isArchived: boolean
  sortOrder: number
}

export interface JournalEntry extends BaseRecord {
  plantId: string
  date: number
  note: string
  tags: string[]            // WorkType 세트는 일지 단계(④)에서 enum 확정
  weatherSnapshot?: unknown // 그날 날씨 박제(JSON), 일지 단계에서 타입 확정
}

export interface JournalPhoto extends BaseRecord {
  ownerId: string           // entry id 또는 plant 대표사진
  blob?: Blob
  driveFileId?: string
}

export interface CareRule extends BaseRecord {
  plantId: string
  careType: CareType
  intervalDays: number
  lastCompletedAt?: number
  nextDueAt: number
  weatherAware: boolean
}
```

`src/data/record.ts`:
```ts
import type { BaseRecord } from './types'

export function newId(): string {
  return crypto.randomUUID()
}

/** 새 레코드의 공통 필드(id·updatedAt·deleted) */
export function baseFields(now: number): BaseRecord {
  return { id: newId(), updatedAt: now, deleted: false }
}
```

`src/data/db.ts`:
```ts
import Dexie, { type Table } from 'dexie'
import type { Area, Plant, JournalEntry, JournalPhoto, CareRule } from './types'

export class GardenDB extends Dexie {
  areas!: Table<Area, string>
  plants!: Table<Plant, string>
  journalEntries!: Table<JournalEntry, string>
  journalPhotos!: Table<JournalPhoto, string>
  careRules!: Table<CareRule, string>

  constructor() {
    super('ordinary-garden')
    // 인덱스 = id(PK) + 조회/동기화에 쓰는 필드.
    // IndexedDB는 boolean을 키로 못 쓰므로 isArchived·deleted는 인덱스 제외(메모리 필터).
    this.version(1).stores({
      areas: 'id, sortOrder, updatedAt',
      plants: 'id, areaId, sortOrder, updatedAt',
      journalEntries: 'id, plantId, date, updatedAt',
      journalPhotos: 'id, ownerId, updatedAt',
      careRules: 'id, plantId, nextDueAt, updatedAt',
    })
  }
}

export const db = new GardenDB()
```

- [ ] **Step 3: 실패하는 areas 테스트**

`src/data/areas.test.ts`:
```ts
import { describe, it, expect, beforeEach } from 'vitest'
import { db } from './db'
import { createArea, listAreas, renameArea, softDeleteArea } from './areas'

beforeEach(async () => {
  await db.areas.clear()
})

describe('areas 리포지토리', () => {
  it('영역을 생성하고 목록에 노출한다', async () => {
    const a = await createArea('베란다', 0)
    const list = await listAreas()
    expect(list.map((x) => x.id)).toContain(a.id)
    expect(a.name).toBe('베란다')
    expect(a.deleted).toBe(false)
  })

  it('이름을 바꾸면 updatedAt이 갱신된다', async () => {
    const a = await createArea('배란다', 0)
    const updated = await renameArea(a.id, '베란다')
    expect(updated.name).toBe('베란다')
    expect(updated.updatedAt).toBeGreaterThanOrEqual(a.updatedAt)
  })

  it('soft delete하면 목록에서 빠지지만 tombstone으로 남는다', async () => {
    const a = await createArea('텃밭', 1)
    await softDeleteArea(a.id)
    const list = await listAreas()
    expect(list.find((x) => x.id === a.id)).toBeUndefined()
    const raw = await db.areas.get(a.id)
    expect(raw?.deleted).toBe(true)
  })
})
```

- [ ] **Step 4: 실패 확인**
Run: `npm test -- src/data/areas.test.ts`
Expected: FAIL (areas 미구현).

- [ ] **Step 5: areas 구현**

`src/data/areas.ts`:
```ts
import { db } from './db'
import { baseFields } from './record'
import type { Area } from './types'

export async function createArea(name: string, sortOrder: number): Promise<Area> {
  const area: Area = { ...baseFields(Date.now()), name, sortOrder }
  await db.areas.add(area)
  return area
}

export async function listAreas(): Promise<Area[]> {
  const all = await db.areas.toArray()
  return all.filter((a) => !a.deleted).sort((x, y) => x.sortOrder - y.sortOrder)
}

export async function renameArea(id: string, name: string): Promise<Area> {
  const existing = await db.areas.get(id)
  if (!existing) throw new Error(`area not found: ${id}`)
  const updated: Area = { ...existing, name, updatedAt: Date.now() }
  await db.areas.put(updated)
  return updated
}

export async function softDeleteArea(id: string): Promise<void> {
  const existing = await db.areas.get(id)
  if (!existing) return
  await db.areas.put({ ...existing, deleted: true, updatedAt: Date.now() })
}
```

- [ ] **Step 6: 통과 확인**
Run: `npm test -- src/data/areas.test.ts`
Expected: PASS (3).

- [ ] **Step 7: 커밋**
```bash
git add -A
git commit -m "feat(web): Dexie 스키마·공통필드·areas 리포지토리"
```

---

## Task 6: PWA (설치 가능 셸)

**Files:** Modify `vite.config.ts`, `index.html` · Create `public/icon-192.png`, `public/icon-512.png`, `public/icon-maskable-512.png`, `public/apple-touch-icon.png`

> 아이콘은 **임시 단색(포레스트그린) placeholder** — 추후 브랜드 아이콘으로 교체. 기준 = 4개 파일이 존재하고 빌드가 통과할 것.

- [ ] **Step 1: vite-plugin-pwa 설치**
```bash
npm install -D vite-plugin-pwa
```

- [ ] **Step 2: 임시 아이콘 생성** (ImageMagick 있으면)
```bash
cd /Users/rakonteto/claude-workspace/ordinary-garden-web
for s in 192 512; do magick -size ${s}x${s} xc:'#2f5d3a' public/icon-${s}.png; done
magick -size 512x512 xc:'#2f5d3a' public/icon-maskable-512.png
magick -size 180x180 xc:'#2f5d3a' public/apple-touch-icon.png
```
ImageMagick이 없으면 다른 방법(node `canvas`, 온라인 변환, 기존 단색 PNG 복사 등)으로 동일 크기 단색 PNG 4개를 만든다. 핵심은 파일 4개 존재.

- [ ] **Step 3: vite.config.ts에 PWA 추가**
```ts
/// <reference types="vitest/config" />
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import { VitePWA } from 'vite-plugin-pwa'

export default defineConfig({
  plugins: [
    react(),
    VitePWA({
      registerType: 'autoUpdate',
      includeAssets: ['apple-touch-icon.png'],
      manifest: {
        name: '보통의 정원',
        short_name: '보통의 정원',
        description: '노지·텃밭 정원과 기상청 날씨',
        lang: 'ko',
        start_url: '.',
        scope: '.',
        display: 'standalone',
        background_color: '#eef1ea',
        theme_color: '#2f5d3a',
        icons: [
          { src: 'icon-192.png', sizes: '192x192', type: 'image/png' },
          { src: 'icon-512.png', sizes: '512x512', type: 'image/png' },
          { src: 'icon-maskable-512.png', sizes: '512x512', type: 'image/png', purpose: 'maskable' },
        ],
      },
    }),
  ],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./src/test/setup.ts'],
  },
})
```

- [ ] **Step 4: index.html `<head>`에 PWA meta 추가**
```html
<meta name="theme-color" content="#2f5d3a" />
<link rel="apple-touch-icon" href="apple-touch-icon.png" />
<meta name="apple-mobile-web-app-capable" content="yes" />
<meta name="apple-mobile-web-app-status-bar-style" content="default" />
```

- [ ] **Step 5: 빌드 확인**
Run: `npm run build`
Expected: `dist/`에 `manifest.webmanifest`·service worker(`sw.js`) 생성.

- [ ] **Step 6: 커밋**
```bash
git add -A
git commit -m "feat(web): PWA manifest·service worker·아이콘"
```

---

## Task 7: GitHub Pages 배포 파이프라인

**Files:** Modify `vite.config.ts`(base) · Create `.github/workflows/deploy.yml`

- [ ] **Step 1: vite base 경로 설정**
`vite.config.ts`의 `defineConfig({ ... })` 최상단에 추가:
```ts
export default defineConfig({
  base: '/ordinary-garden-web/',
  plugins: [
    // ...기존 react(), VitePWA(...) 유지
  ],
  // ...기존 test 설정 유지
})
```
(HashRouter라 SPA 404 fallback 불필요. base는 프로젝트 페이지 하위경로.)

- [ ] **Step 2: 배포 워크플로 작성**
`.github/workflows/deploy.yml`:
```yaml
name: Deploy to GitHub Pages
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: true
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run build
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist
  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
```

- [ ] **Step 3: 로컬 빌드 재확인**
Run: `npm run build`
Expected: `dist/index.html`의 자산 경로가 `/ordinary-garden-web/...`로 반영.

- [ ] **Step 4: 커밋**
```bash
git add -A
git commit -m "ci(web): GitHub Pages 배포 워크플로·base 경로"
```

> **배포 활성화(사용자 작업, 이 계획 범위 밖):** GitHub에 공개 repo `ordinary-garden-web` 생성 → `git remote add origin …` → `git push -u origin main` → repo Settings ▸ Pages ▸ Source = "GitHub Actions". 이후 push마다 자동 배포. 이 계획은 코드·워크플로만 준비하고 repo 생성·push는 포함하지 않는다.

---

## 완료 기준 (기반 단계)
- `npm run dev` → 3탭 셸이 세이지/글래스 디자인으로 뜨고 탭 이동 동작.
- `npm test` → smoke·App·areas 테스트 통과.
- `npm run build` → 타입체크 통과 + PWA(manifest·sw) 산출 + base 경로 반영.
- 후속 계획(② 날씨 ~ ⑥ 동기화)이 이 위에 기능을 얹을 준비 완료.
